# Git Commands Log

## Repository setup
```bash
git init
git status

## Staging and committing

git add .
git commit -m "Initial commit"

## Branching

git branch feature-setup
git checkout feature-setup

## Merging

git checkout main
git merge feature-setup
